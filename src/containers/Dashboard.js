import React, { Fragment } from 'react';
import NavBar from '../components/NavBar';

export const Dashboard = () => {
    return (
        <Fragment>
            <NavBar />
        </Fragment>
    );
};
