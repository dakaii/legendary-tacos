import React, { Fragment } from 'react';
import Header from '../components/Header';

export const Landing = () => {
    return (
        <Fragment>
            <Header />
        </Fragment>
    );
};
